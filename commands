sh steve@stapp02 # here app server 2 password needed
sudo mkdir -p /scripts # if not available
sudo sudo vi /scripts/beta_backup.sh
sudo chown steve:steve /scripts/ # give ownership to steve
sudo chown steve:steve /scripts/beta_backup.sh # give ownership to steve
sudo chmod +x /scripts/beta_backup.sh # give execute permission & just copy paste
sudo mkdir -p /var/www/html/beta # if not available
sudo chown steve:steve /var/www/html/beta # give ownership to steve
sudo chmod -R 755 /var/www/html/beta # give permission to steve

ssh-keygen -t rsa -b 2048
ssh-copy-id clint@stbkp01

sudo /scripts/beta_backup.sh
